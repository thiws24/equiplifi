{
  "title": "Confirmation",
  "description": "confirmed or not confirmed",
  "type": "object",
  "required": [
    "reservation_confirmation"
  ],
  "properties": {
    "reservation_confirmation": {
      "type": "string",
      "title": "Confirmation",
      "enum": ["confirmed", "not confirmed"],
      "default": "confirmed"
    }
  }
}